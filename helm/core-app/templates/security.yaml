{{- if .Values.security.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: security-secret
  namespace: {{ .Values.security.namespace.name }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.security.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: security
  namespace: {{ .Values.security.namespace.name }}
  labels:
    app: security
spec:
  replicas: {{ .Values.security.replicaCount }}
  selector:
    matchLabels:
      app: security
  template:
    metadata:
      labels:
        app: security
    spec:
      containers:
      - name: security
        image: {{ .Values.global.imageRegistry }}/{{ .Values.global.imageOrganization }}/{{ .Values.security.image.repository }}:{{ .Values.security.image.tag }}
        imagePullPolicy: {{ .Values.global.imagePullPolicy }}
        ports:
        - containerPort: {{ .Values.security.service.targetPort }}
          name: http
        env:
        {{- range .Values.security.env }}
        - name: {{ .name }}
          value: {{ .value | quote }}
        {{- end }}
        {{- range $key, $value := .Values.security.secrets }}
        - name: {{ $key }}
          valueFrom:
            secretKeyRef:
              name: security-secret
              key: {{ $key }}
        {{- end }}
        resources:
          {{- toYaml .Values.security.resources | nindent 10 }}
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: security
  namespace: {{ .Values.security.namespace.name }}
  labels:
    app: security
spec:
  type: {{ .Values.security.service.type }}
  ports:
  - port: {{ .Values.security.service.port }}
    targetPort: {{ .Values.security.service.targetPort }}
    protocol: TCP
    name: http
  selector:
    app: security
{{- end }}
